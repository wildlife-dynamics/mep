# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Predation Report
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.groupby import set_groupers as set_groupers
from ecoscope_workflows_core.tasks.groupby import split_groups as split_groups
from ecoscope_workflows_core.tasks.io import persist_text as persist_text
from ecoscope_workflows_core.tasks.results import (
    create_map_widget_single_view as create_map_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import (
    create_plot_widget_single_view as create_plot_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_core.tasks.results import (
    merge_widget_views as merge_widget_views,
)
from ecoscope_workflows_core.tasks.skip import (
    any_dependency_skipped as any_dependency_skipped,
)
from ecoscope_workflows_core.tasks.skip import any_is_empty_df as any_is_empty_df
from ecoscope_workflows_core.tasks.skip import never as never
from ecoscope_workflows_core.tasks.transformation import (
    add_temporal_index as add_temporal_index,
)
from ecoscope_workflows_core.tasks.transformation import map_columns as map_columns
from ecoscope_workflows_core.tasks.transformation import sort_values as sort_values
from ecoscope_workflows_ext_big_life.tasks import aggregate_by as aggregate_by
from ecoscope_workflows_ext_big_life.tasks import crosstab_summary as crosstab_summary
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_multi_bar_time_series as draw_custom_multi_bar_time_series,
)
from ecoscope_workflows_ext_big_life.tasks import (
    draw_custom_multi_line_time_series as draw_custom_multi_line_time_series,
)
from ecoscope_workflows_ext_big_life.tasks import (
    generate_density_grid as generate_density_grid,
)
from ecoscope_workflows_ext_big_life.tasks import get_color_map as get_color_map
from ecoscope_workflows_ext_big_life.tasks import (
    map_color_column_value as map_color_column_value,
)
from ecoscope_workflows_ext_big_life.tasks import (
    select_time_frequency as select_time_frequency,
)
from ecoscope_workflows_ext_custom.tasks.io import html_to_png as html_to_png
from ecoscope_workflows_ext_custom.tasks.io import load_df as load_df
from ecoscope_workflows_ext_custom.tasks.io import (
    persist_df_wrapper as persist_df_wrapper,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_geojson_layer as create_geojson_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_scatterplot_layer as create_scatterplot_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import draw_map as draw_map
from ecoscope_workflows_ext_custom.tasks.results import (
    set_base_maps_pydeck as set_base_maps_pydeck,
)
from ecoscope_workflows_ext_custom.tasks.spatial_ops import (
    reproject_gdf as reproject_gdf,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_null_geometry as drop_null_geometry,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    exclude_row_values as exclude_row_values,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    filter_row_values as filter_row_values,
)
from ecoscope_workflows_ext_ecoscope.tasks.results import (
    draw_pie_chart as draw_pie_chart,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_classification as apply_classification,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_color_map as apply_color_map,
)
from ecoscope_workflows_ext_mep.tasks import custom_map_column as custom_map_column
from ecoscope_workflows_ext_mep.tasks import draw_custom_heatmap as draw_custom_heatmap
from ecoscope_workflows_ext_mep.tasks import (
    herder_effectiveness as herder_effectiveness,
)
from ecoscope_workflows_ext_mep.tasks import (
    load_local_tabular_file as load_local_tabular_file,
)
from ecoscope_workflows_ext_mep.tasks import (
    species_by_ranch_matrix as species_by_ranch_matrix,
)
from ecoscope_workflows_ext_mep.tasks import utm_to_4326 as utm_to_4326
from ecoscope_workflows_ext_mnc.tasks import convert_to_int as convert_to_int
from ecoscope_workflows_ext_mnc.tasks import (
    exclude_geom_outliers as exclude_geom_outliers,
)
from ecoscope_workflows_ext_mnc.tasks import map_column_values as map_column_values
from ecoscope_workflows_ext_mnc.tasks import (
    replace_missing_with_label as replace_missing_with_label,
)
from ecoscope_workflows_ext_ste.tasks import (
    combine_deckgl_map_layers as combine_deckgl_map_layers,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_custom_text_layer as create_custom_text_layer,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_deckgl_layer_from_gdf as create_deckgl_layer_from_gdf,
)
from ecoscope_workflows_ext_ste.tasks import (
    fetch_and_persist_file as fetch_and_persist_file,
)
from ecoscope_workflows_ext_ste.tasks import get_gdf_geom_type as get_gdf_geom_type
from ecoscope_workflows_ext_ste.tasks import view_state_deck_gdf as view_state_deck_gdf
from ecoscope_workflows_ext_ste.tasks import zip_groupbykey as zip_groupbykey

# %% [markdown]
# ## Set workflow details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Define analysis time range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
    time_format=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_range_params)
    .call()
)


# %% [markdown]
# ## Configure grouping strategy

# %%
# parameters

groupers_params = dict(
    groupers=...,
)

# %%
# call the task


groupers = (
    set_groupers.set_task_instance_id("groupers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**groupers_params)
    .call()
)


# %% [markdown]
# ## Select time frequency for temporal charts

# %%
# parameters

time_frequency_params = dict(
    time_frequency=...,
)

# %%
# call the task


time_frequency = (
    select_time_frequency.set_task_instance_id("time_frequency")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_frequency_params)
    .call()
)


# %% [markdown]
# ## Configure base map layers

# %%
# parameters

configure_base_maps_params = dict()

# %%
# call the task


configure_base_maps = (
    set_base_maps_pydeck.set_task_instance_id("configure_base_maps")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        base_maps=[
            {
                "url": "https://server.arcgisonline.com/arcgis/rest/services/Elevation/World_Hillshade/MapServer/tile/{z}/{y}/{x}",
                "opacity": 1,
                "max_zoom": 20,
            },
            {
                "url": "https://server.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer/tile/{z}/{y}/{x}",
                "opacity": 0.15,
                "max_zoom": 20,
            },
        ],
        **configure_base_maps_params,
    )
    .call()
)


# %% [markdown]
# ## Download amboseli group ranches

# %%
# parameters

persist_ambo_gpkg_params = dict()

# %%
# call the task


persist_ambo_gpkg = (
    fetch_and_persist_file.set_task_instance_id("persist_ambo_gpkg")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/fcy57d5x5y67gh0xje6rw/lg_group_ranch_boundaries.gpkg?rlkey=yte99xhkte7f1r7n9nemgy7p0&st=sxx1emc1&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_ambo_gpkg_params,
    )
    .call()
)


# %% [markdown]
# ## Download hotspot areas gpkg

# %%
# parameters

persist_hotspot_areas_params = dict()

# %%
# call the task


persist_hotspot_areas = (
    fetch_and_persist_file.set_task_instance_id("persist_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/nlozcti0oqhj6bzvsr46g/lg_conflict_hotspots.gpkg?rlkey=giwdizp1j6e24btgh48uxf5v0&st=jivblame&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_hotspot_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Download protected areas gpkg

# %%
# parameters

persist_protected_gpkg_params = dict()

# %%
# call the task


persist_protected_gpkg = (
    fetch_and_persist_file.set_task_instance_id("persist_protected_gpkg")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/i5yczgyln3zh1n8c4ppl5/lg_protected_areas.gpkg?rlkey=5ea21haq2tmsmx7g502p3qag5&st=zt6ztcku&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_protected_gpkg_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli group ranches

# %%
# parameters

load_ambo_group_ranches_params = dict()

# %%
# call the task


load_ambo_group_ranches = (
    load_df.set_task_instance_id("load_ambo_group_ranches")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_ambo_gpkg,
        layer=None,
        deserialize_json=False,
        **load_ambo_group_ranches_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli group ranches

# %%
# parameters

load_hotspot_areas_params = dict()

# %%
# call the task


load_hotspot_areas = (
    load_df.set_task_instance_id("load_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_hotspot_areas,
        layer=None,
        deserialize_json=False,
        **load_hotspot_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Load protected areas

# %%
# parameters

load_protected_areas_params = dict()

# %%
# call the task


load_protected_areas = (
    load_df.set_task_instance_id("load_protected_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_protected_gpkg,
        layer=None,
        deserialize_json=False,
        **load_protected_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for ambo layers

# %%
# parameters

reproject_ambo_boundaries_params = dict()

# %%
# call the task


reproject_ambo_boundaries = (
    reproject_gdf.set_task_instance_id("reproject_ambo_boundaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_ambo_group_ranches,
        target_crs="EPSG:4326",
        **reproject_ambo_boundaries_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for hotspot areas

# %%
# parameters

reproject_hotspot_areas_params = dict()

# %%
# call the task


reproject_hotspot_areas = (
    reproject_gdf.set_task_instance_id("reproject_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_hotspot_areas, target_crs="EPSG:4326", **reproject_hotspot_areas_params
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for protected areas

# %%
# parameters

reproject_protected_areas_params = dict()

# %%
# call the task


reproject_protected_areas = (
    reproject_gdf.set_task_instance_id("reproject_protected_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_protected_areas,
        target_crs="EPSG:4326",
        **reproject_protected_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Annotate amboseli layers with geom type

# %%
# parameters

annotate_ambo_layers_params = dict()

# %%
# call the task


annotate_ambo_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_ambo_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_ambo_boundaries, **annotate_ambo_layers_params)
    .call()
)


# %% [markdown]
# ## Annotate hotspot layers with geom type

# %%
# parameters

annotate_hotspot_layers_params = dict()

# %%
# call the task


annotate_hotspot_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_hotspot_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_hotspot_areas, **annotate_hotspot_layers_params)
    .call()
)


# %% [markdown]
# ## Annotate protected areas with geom type

# %%
# parameters

annotate_protected_layers_params = dict()

# %%
# call the task


annotate_protected_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_protected_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_protected_areas, **annotate_protected_layers_params)
    .call()
)


# %% [markdown]
# ## Create layer for amboseli group ranch

# %%
# parameters

custom_amboseli_layer_params = dict()

# %%
# call the task


custom_amboseli_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_amboseli_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_ambo_layers,
        style={
            "get_line_color": [169, 169, 169],
            "get_fill_color": [169, 169, 169],
            "get_line_width": 4.5,
            "opacity": 0.55,
            "extruded": False,
            "stroked": True,
            "filled": False,
        },
        legend={
            "title": "Map layers",
            "values": [{"label": "Group ranch boundaries", "color": "#a9a9a9"}],
        },
        **custom_amboseli_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create layer for hotspot areas

# %%
# parameters

custom_hotspot_layer_params = dict()

# %%
# call the task


custom_hotspot_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_hotspot_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_hotspot_layers,
        style={
            "get_line_color": [220, 20, 60],
            "get_fill_color": [220, 20, 60],
            "get_radius": 2.55,
            "get_line_width": 1.95,
            "opacity": 0.75,
            "extruded": False,
            "stroked": True,
            "filled": True,
        },
        legend={
            "title": "",
            "values": [{"label": "Hotspot areas", "color": "#dc143c"}],
        },
        **custom_hotspot_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create layer for protected areas

# %%
# parameters

custom_protected_layer_params = dict()

# %%
# call the task


custom_protected_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_protected_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_protected_layers,
        style={
            "get_line_color": [77, 102, 0],
            "get_fill_color": [77, 102, 0],
            "get_line_width": 1.95,
            "opacity": 0.35,
            "extruded": False,
            "stroked": True,
            "filled": True,
        },
        legend={
            "title": "",
            "values": [{"label": "National parks and reserves", "color": "#4d6600"}],
        },
        **custom_protected_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create text layer for conflict hotspots

# %%
# parameters

create_hotspot_text_layer_params = dict()

# %%
# call the task


create_hotspot_text_layer = (
    create_custom_text_layer.set_task_instance_id("create_hotspot_text_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        geodataframe=reproject_hotspot_areas,
        layer_style={
            "get_text": "name",
            "get_color": [20, 20, 20, 255],
            "get_size": 1000,
            "size_units": "meters",
            "size_min_pixels": 40,
            "size_max_pixels": 75,
            "size_scale": 1.25,
            "font_family": "Arial",
            "font_weight": "normal",
            "get_text_anchor": "middle",
            "get_alignment_baseline": "center",
            "billboard": True,
            "background_padding": [4, 8],
            "pickable": True,
            "auto_highlight": False,
        },
        use_centroid=True,
        legend=None,
        **create_hotspot_text_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Load local tabular file

# %%
# parameters

load_local_lg_file_params = dict(
    file_path=...,
)

# %%
# call the task


load_local_lg_file = (
    load_local_tabular_file.set_task_instance_id("load_local_lg_file")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(encoding="latin-1", **load_local_lg_file_params)
    .call()
)


# %% [markdown]
# ## Map columns

# %%
# parameters

map_predation_columns_params = dict()

# %%
# call the task


map_predation_columns = (
    map_columns.set_task_instance_id("map_predation_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=load_local_lg_file,
        drop_columns=[
            "Month ",
            "Year",
            "Verified by Biologist",
            "General Comments",
            "Observations",
            "Unknown/ Other",
            "Specific Area",
        ],
        retain_columns=[],
        rename_columns={
            "FID": "id",
            "Date today (MM/DD/YYYY)": "date",
            "Species killed": "species_killed",
            "total killed": "total_killed",
            "Species Injured": "species_injured",
            "total injured": "total_injured",
            "Was the prey livestock (Yes/No)": "is_livestock",
            "Depredation from Boma or Bush?": "boma_or_bush",
            "If bush, were livestock lost? YES/ NO/ NA/ UNKNOWN": "livestock_lost_in_bush",
            "If bush & not lost, were livestock with herder? YES/ NO/ UNKNOWN": "livestock_not_lost_in_bush_herder_present",
            "Group Ranch (MGR, EGR, OGR, Other, Unknown)": "ranch",
            "Community hunt": "community_hunt",
            "Mock hunt": "mock_hunt",
            "Scared by herders- not with spears": "scared_by_herders",
        },
        raise_if_not_found=False,
        **map_predation_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Add temporal index

# %%
# parameters

add_temporal_index_to_df_params = dict()

# %%
# call the task


add_temporal_index_to_df = (
    add_temporal_index.set_task_instance_id("add_temporal_index_to_df")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_predation_columns,
        time_col="date",
        groupers=groupers,
        cast_to_datetime=True,
        format="mixed",
        **add_temporal_index_to_df_params,
    )
    .call()
)


# %% [markdown]
# ## Replace missing with label

# %%
# parameters

replace_values_with_unknown_params = dict()

# %%
# call the task


replace_values_with_unknown = (
    replace_missing_with_label.set_task_instance_id("replace_values_with_unknown")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=add_temporal_index_to_df,
        columns=[
            "species_killed",
            "is_livestock",
            "livestock_lost_in_bush",
            "species_injured",
            "livestock_not_lost_in_bush_herder_present",
            "ranch",
            "boma_or_bush",
        ],
        label="Unknown",
        **replace_values_with_unknown_params,
    )
    .call()
)


# %% [markdown]
# ## Convert columns to integer

# %%
# parameters

convert_cols_int_params = dict()

# %%
# call the task


convert_cols_int = (
    convert_to_int.set_task_instance_id("convert_cols_int")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=replace_values_with_unknown,
        columns=[
            "total_killed",
            "total_injured",
            "community_hunt",
            "mock_hunt",
            "scared_by_herders",
        ],
        errors="coerce",
        fill_value=0,
        inplace=True,
        **convert_cols_int_params,
    )
    .call()
)


# %% [markdown]
# ## Map prey column

# %%
# parameters

map_prey_column_params = dict()

# %%
# call the task


map_prey_column = (
    map_column_values.set_task_instance_id("map_prey_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_cols_int,
        columns=["species_killed"],
        value_map={
            "cow": "Cow",
            "bull": "Bull",
            "zebra": "Zebra",
            "Zebra ": "Zebra",
            "shoat": "Shoat",
            "sheep": "Sheep",
            "goat": "Goat",
            "giraffe": "Giraffe",
            "Wildebeest ": "Wildebeest",
            "Cow (calf)": "Cow",
            "Bull": "Cow",
            "Giraffe (foal)": "Giraffe",
            "Zebra (foal)": "Zebra",
            "Eland (calf)": "Eland",
            "Wildebeest (calf)": "Wildebeest",
            "Donkey (foal)": "Donkey",
        },
        inplace=True,
        **map_prey_column_params,
    )
    .call()
)


# %% [markdown]
# ## Map prey value column

# %%
# parameters

map_prey_column_value_params = dict()

# %%
# call the task


map_prey_column_value = (
    map_column_values.set_task_instance_id("map_prey_column_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_prey_column,
        columns=["species_injured"],
        inplace=True,
        value_map={
            "cow": "Cow",
            "shoat": "Shoat",
            "donkey": "Donkey",
            "Sheep-kid": "Sheep",
            "Cow (calf)": "Cow",
        },
        **map_prey_column_value_params,
    )
    .call()
)


# %% [markdown]
# ## Map prey consumed

# %%
# parameters

map_prey_consumed_params = dict()

# %%
# call the task


map_prey_consumed = (
    map_column_values.set_task_instance_id("map_prey_consumed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_prey_column_value,
        columns=["is_livestock"],
        inplace=True,
        value_map={
            "YES": "Yes",
            "NO": "No",
            "Yes": "Yes",
            "No": "No",
            "No prey, but lion disciplined": "No",
        },
        **map_prey_consumed_params,
    )
    .call()
)


# %% [markdown]
# ## Map boma values

# %%
# parameters

map_boma_values_params = dict()

# %%
# call the task


map_boma_values = (
    map_column_values.set_task_instance_id("map_boma_values")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_prey_consumed,
        columns=["boma_or_bush"],
        inplace=True,
        value_map={
            "bush": "Bush",
            "Unknown": "Unknown",
            "BOMA": "Boma",
            "Bush": "Bush",
        },
        **map_boma_values_params,
    )
    .call()
)


# %% [markdown]
# ## Map livestock columns

# %%
# parameters

map_livestock_columns_params = dict()

# %%
# call the task


map_livestock_columns = (
    map_column_values.set_task_instance_id("map_livestock_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_boma_values,
        columns=["livestock_lost_in_bush", "livestock_not_lost_in_bush_herder_present"],
        inplace=True,
        value_map={
            "yes": "Yes",
            "Unknown": "Unknown",
            "Yes": "Yes",
            "unknown": "Unknown",
            "No": "No",
            "NO": "No",
            "no": "No",
            "YES": "Yes",
            "UNKNOWN": "Unknown",
        },
        **map_livestock_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Map ranch column

# %%
# parameters

map_ranch_column_params = dict()

# %%
# call the task


map_ranch_column = (
    map_column_values.set_task_instance_id("map_ranch_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_livestock_columns,
        columns=["ranch"],
        inplace=True,
        value_map={
            "EGR": "EGR",
            "MGR": "MGR",
            "OGR": "OGR",
            "Other-OGR": "OGR",
            "KUKU": "Kuku",
            "Other-KUKU": "Kuku",
            "Other KUKU": "Kuku",
            "Other- Kuku": "Kuku",
            "Other - Kuku GR": "Kuku",
            "Kuku Group ranch": "Kuku",
            "Other - Kaputei": "Kaputei",
            "Other-Kaputei": "Kaputei",
            "Other - Matapato": "Matapato",
            "Other-Matopato": "Matapato",
            "Other - Osilalei Group Ranch": "Osilalei",
            "Other-Kimana": "Kimana",
            "Tanzania": "Tanzania",
            "Other - TZ": "Tanzania",
            "Other-Kunchu": "Other",
            "Other": "Other",
            "Unknown": "Unknown",
            "unknown": "Unknown",
            "UNKNOWN": "Unknown",
        },
        **map_ranch_column_params,
    )
    .call()
)


# %% [markdown]
# ## Convert df to gdf

# %%
# parameters

convert_df_gdf_params = dict()

# %%
# call the task


convert_df_gdf = (
    utm_to_4326.set_task_instance_id("convert_df_gdf")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_ranch_column,
        utm_epsg=32737,
        x_col="GPS X (UTM)",
        y_col="GPS Y (UTM)",
        **convert_df_gdf_params,
    )
    .call()
)


# %% [markdown]
# ## Map gdf columns

# %%
# parameters

map_gdf_columns_params = dict()

# %%
# call the task


map_gdf_columns = (
    map_columns.set_task_instance_id("map_gdf_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_df_gdf,
        drop_columns=["GPS X (UTM)", "GPS Y (UTM)", "None"],
        retain_columns=[],
        rename_columns={},
        raise_if_not_found=False,
        **map_gdf_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Map is_livestock columns

# %%
# parameters

map_is_livestock_cols_params = dict()

# %%
# call the task


map_is_livestock_cols = (
    custom_map_column.set_task_instance_id("map_is_livestock_cols")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_gdf_columns,
        column="is_livestock",
        inplace_col=True,
        mapping={
            "YES": True,
            "Yes": True,
            "yes": True,
            "NO": False,
            "No": False,
            "no": False,
            "No prey, but lion disciplined": False,
        },
        **map_is_livestock_cols_params,
    )
    .call()
)


# %% [markdown]
# ## Filter livestock gdf values

# %%
# parameters

filter_livestock_gdf_params = dict()

# %%
# call the task


filter_livestock_gdf = (
    filter_row_values.set_task_instance_id("filter_livestock_gdf")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_is_livestock_cols,
        column="species_killed",
        values=["Cow", "Goat", "Shoat", "Donkey", "Sheep", "Bull", "Dog"],
        **filter_livestock_gdf_params,
    )
    .call()
)


# %% [markdown]
# ## Exclude values from species_killed column

# %%
# parameters

exclude_wild_species_params = dict()

# %%
# call the task


exclude_wild_species = (
    exclude_row_values.set_task_instance_id("exclude_wild_species")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=filter_livestock_gdf,
        column="species_killed",
        values=[
            "Zebra",
            "Wildebeest",
            "Eland",
            "Giraffe",
            "Hyena - spotted",
            "Oryx",
            "Ostrich",
            "Porcupine",
            "Thompson's Gazelle",
            "Grant's Gazelle",
            "Lesser kudu",
            "Buffalo",
            "Gazelle",
        ],
        **exclude_wild_species_params,
    )
    .call()
)


# %% [markdown]
# ## Get species colormap

# %%
# parameters

get_species_colors_params = dict()

# %%
# call the task


get_species_colors = (
    get_color_map.set_task_instance_id("get_species_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=exclude_wild_species,
        col="species_killed",
        color_dict={
            "Cow": "#fcb5ac",
            "Bull": "#b8d4e8",
            "Goat": "#d4cfe6",
            "Shoat": "#cfc7bd",
            "Sheep": "#fff5b1",
            "Donkey": "#e8c9a8",
            "Dog": "#c2d8c5",
            "Zebra": "#f49990",
            "Wildebeest": "#bcd9de",
            "Buffalo": "#9cbac8",
            "Eland": "#d4edbc",
            "Giraffe": "#e4ecb5",
            "Oryx": "#c9e6d1",
            "Lesser kudu": "#ddc4ae",
            "Gazelle": "#ffd3a5",
            "Thompson's Gazelle": "#d8b8d7",
            "Grant's Gazelle": "#dec4de",
            "Ostrich": "#a8e0d0",
            "Porcupine": "#ffe0b8",
            "Hyena - spotted": "#cfc7bd",
            "Unknown": "#e0e0dc",
        },
        **get_species_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Get boma or bush colors

# %%
# parameters

get_boma_or_bush_colors_params = dict()

# %%
# call the task


get_boma_or_bush_colors = (
    get_color_map.set_task_instance_id("get_boma_or_bush_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=exclude_wild_species,
        col="boma_or_bush",
        color_dict={"Boma": "#a8d0e6", "Bush": "#c8e6c9", "Unknown": "#e0e0dc"},
        **get_boma_or_bush_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map ranch colors

# %%
# parameters

map_ranch_colors_params = dict()

# %%
# call the task


map_ranch_colors = (
    get_color_map.set_task_instance_id("map_ranch_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=exclude_wild_species,
        col="ranch",
        color_dict={
            "EGR": "#fcb5ac",
            "MGR": "#ffd3a5",
            "OGR": "#fff5b1",
            "Kuku": "#d4edbc",
            "Kimana": "#a8e0d0",
            "Kaputei": "#b8d4e8",
            "Matapato": "#d4cfe6",
            "Osilalei": "#e0c4de",
            "Tanzania": "#c4b7dc",
            "Other": "#cfc7bd",
            "Unknown": "#e0e0dc",
        },
        **map_ranch_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map color column value to ranch

# %%
# parameters

map_color_ranch_params = dict()

# %%
# call the task


map_color_ranch = (
    map_color_column_value.set_task_instance_id("map_color_ranch")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=exclude_wild_species,
        col="ranch",
        mapping={
            "EGR": "#fcb5ac",
            "MGR": "#ffd3a5",
            "OGR": "#fff5b1",
            "Kuku": "#d4edbc",
            "Kimana": "#a8e0d0",
            "Kaputei": "#b8d4e8",
            "Matapato": "#d4cfe6",
            "Osilalei": "#e0c4de",
            "Tanzania": "#c4b7dc",
            "Other": "#cfc7bd",
            "Unknown": "#e0e0dc",
        },
        new_col="ranch_colors",
        **map_color_ranch_params,
    )
    .call()
)


# %% [markdown]
# ## Map color column to boma_or_bush

# %%
# parameters

map_color_boma_or_bush_params = dict()

# %%
# call the task


map_color_boma_or_bush = (
    map_color_column_value.set_task_instance_id("map_color_boma_or_bush")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_color_ranch,
        col="boma_or_bush",
        mapping={"Boma": "#a8d0e6", "Bush": "#c8e6c9", "Unknown": "#e0e0dc"},
        new_col="boma_or_bush_colors",
        **map_color_boma_or_bush_params,
    )
    .call()
)


# %% [markdown]
# ## Map color column to species killed

# %%
# parameters

map_color_species_killed_params = dict()

# %%
# call the task


map_color_species_killed = (
    map_color_column_value.set_task_instance_id("map_color_species_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_color_boma_or_bush,
        col="species_killed",
        mapping={
            "Cow": "#fcb5ac",
            "Bull": "#b8d4e8",
            "Goat": "#d4cfe6",
            "Shoat": "#cfc7bd",
            "Sheep": "#fff5b1",
            "Donkey": "#e8c9a8",
            "Dog": "#c2d8c5",
            "Zebra": "#f49990",
            "Wildebeest": "#bcd9de",
            "Buffalo": "#9cbac8",
            "Eland": "#d4edbc",
            "Giraffe": "#e4ecb5",
            "Oryx": "#c9e6d1",
            "Lesser kudu": "#ddc4ae",
            "Gazelle": "#ffd3a5",
            "Thompson's Gazelle": "#d8b8d7",
            "Grant's Gazelle": "#dec4de",
            "Ostrich": "#a8e0d0",
            "Porcupine": "#ffe0b8",
            "Hyena - spotted": "#cfc7bd",
            "Unknown": "#e0e0dc",
        },
        new_col="species_killed_colors",
        **map_color_species_killed_params,
    )
    .call()
)


# %% [markdown]
# ## Split events by group

# %%
# parameters

split_events_by_group_params = dict()

# %%
# call the task


split_events_by_group = (
    split_groups.set_task_instance_id("split_events_by_group")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_color_species_killed, groupers=groupers, **split_events_by_group_params
    )
    .call()
)


# %% [markdown]
# ## Crosstab summary of total livestock killed by ranch

# %%
# parameters

total_livestock_killed_ranch_params = dict()

# %%
# call the task


total_livestock_killed_ranch = (
    crosstab_summary.set_task_instance_id("total_livestock_killed_ranch")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        index_col="species_killed",
        column_col="ranch",
        value_col="total_killed",
        aggfunc="sum",
        reset_index=True,
        margins=True,
        margins_name="Total",
        **total_livestock_killed_ranch_params,
    )
    .mapvalues(argnames=["data"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist table as csv

# %%
# parameters

persist_total_livestock_killed_params = dict()

# %%
# call the task


persist_total_livestock_killed = (
    persist_df_wrapper.set_task_instance_id("persist_total_livestock_killed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_prefix="total_livestock_killed_by_ranch",
        filetypes=["csv"],
        sanitize=False,
        **persist_total_livestock_killed_params,
    )
    .mapvalues(argnames=["df"], argvalues=total_livestock_killed_ranch)
)


# %% [markdown]
# ## Location of attack

# %%
# parameters

location_of_attack_params = dict()

# %%
# call the task


location_of_attack = (
    aggregate_by.set_task_instance_id("location_of_attack")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        group_col="boma_or_bush",
        agg_func="count",
        agg_col_name="Count",
        agg_col=None,
        sort="asc",
        add_percent=True,
        **location_of_attack_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist table as csv

# %%
# parameters

persist_agg_location_params = dict()

# %%
# call the task


persist_agg_location = (
    persist_df_wrapper.set_task_instance_id("persist_agg_location")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_prefix="location_of_attack",
        filetypes=["csv"],
        sanitize=False,
        **persist_agg_location_params,
    )
    .mapvalues(argnames=["df"], argvalues=location_of_attack)
)


# %% [markdown]
# ## Herder effectiveness

# %%
# parameters

herder_location_effectiveness_params = dict()

# %%
# call the task


herder_location_effectiveness = (
    herder_effectiveness.set_task_instance_id("herder_location_effectiveness")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**herder_location_effectiveness_params)
    .mapvalues(argnames=["df"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist table as csv

# %%
# parameters

persist_herder_effectiveness_params = dict()

# %%
# call the task


persist_herder_effectiveness = (
    persist_df_wrapper.set_task_instance_id("persist_herder_effectiveness")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_prefix="herder_effectiveness",
        filetypes=["csv"],
        sanitize=False,
        **persist_herder_effectiveness_params,
    )
    .mapvalues(argnames=["df"], argvalues=herder_location_effectiveness)
)


# %% [markdown]
# ## Livestock species by ranch matrix

# %%
# parameters

livestock_species_ranch_matrix_params = dict()

# %%
# call the task


livestock_species_ranch_matrix = (
    species_by_ranch_matrix.set_task_instance_id("livestock_species_ranch_matrix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        livestock_only=True, normalize=False, **livestock_species_ranch_matrix_params
    )
    .mapvalues(argnames=["df"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist table as csv

# %%
# parameters

persist_species_matrix_params = dict()

# %%
# call the task


persist_species_matrix = (
    persist_df_wrapper.set_task_instance_id("persist_species_matrix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_prefix="species_ranch_matrix",
        filetypes=["csv"],
        sanitize=False,
        **persist_species_matrix_params,
    )
    .mapvalues(argnames=["df"], argvalues=livestock_species_ranch_matrix)
)


# %% [markdown]
# ## Draw total livestock killed pie chart

# %%
# parameters

total_livestock_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


total_livestock_pie_chart = (
    draw_pie_chart.set_task_instance_id("total_livestock_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        value_column="total_killed",
        label_column="species_killed",
        color_column="species_killed_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **total_livestock_pie_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist total livestock killed pie chart

# %%
# parameters

persist_total_livestock_pie_params = dict()

# %%
# call the task


persist_total_livestock_pie = (
    persist_text.set_task_instance_id("persist_total_livestock_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_killed_pie_chart",
        **persist_total_livestock_pie_params,
    )
    .mapvalues(argnames=["text"], argvalues=total_livestock_pie_chart)
)


# %% [markdown]
# ## Draw total livestock killed pie chart

# %%
# parameters

ranch_pie_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


ranch_pie_chart = (
    draw_pie_chart.set_task_instance_id("ranch_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        value_column="total_killed",
        label_column="ranch",
        color_column="ranch_colors",
        plot_style={"textinfo": "percent+label+value"},
        layout_style={"font_size": 15, "font_style": "normal", "showlegend": True},
        **ranch_pie_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist total livestock killed by ranch pie chart

# %%
# parameters

persist_ranch_livestock_pie_params = dict()

# %%
# call the task


persist_ranch_livestock_pie = (
    persist_text.set_task_instance_id("persist_ranch_livestock_pie")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_killed_by_ranch_pie_chart",
        **persist_ranch_livestock_pie_params,
    )
    .mapvalues(argnames=["text"], argvalues=ranch_pie_chart)
)


# %% [markdown]
# ## Draw species heatmap by ranch

# %%
# parameters

species_heatmap_params = dict(
    widget_id=...,
)

# %%
# call the task


species_heatmap = (
    draw_custom_heatmap.set_task_instance_id("species_heatmap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="species_killed",
        y_axis="ranch",
        value_column="total_killed",
        agg_function="sum",
        time_frequency=None,
        time_column=None,
        time_axis="x",
        normalize=False,
        colorscale="YlOrRd",
        show_values=True,
        x_order=None,
        y_order=None,
        value_label=None,
        layout_style=None,
        **species_heatmap_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist species ranch heatmap

# %%
# parameters

persist_species_ranch_heatmap_params = dict()

# %%
# call the task


persist_species_ranch_heatmap = (
    persist_text.set_task_instance_id("persist_species_ranch_heatmap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="species_by_ranch_heatmap",
        **persist_species_ranch_heatmap_params,
    )
    .mapvalues(argnames=["text"], argvalues=species_heatmap)
)


# %% [markdown]
# ## Draw species heatmap by time frequency

# %%
# parameters

species_time_heatmap_params = dict(
    widget_id=...,
)

# %%
# call the task


species_time_heatmap = (
    draw_custom_heatmap.set_task_instance_id("species_time_heatmap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="species_killed",
        y_axis="species_killed",
        value_column="total_killed",
        agg_function="sum",
        time_frequency=time_frequency,
        time_column="date",
        time_axis="x",
        normalize=False,
        colorscale="YlOrRd",
        show_values=True,
        x_order=None,
        y_order=None,
        value_label=None,
        layout_style=None,
        **species_time_heatmap_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist species ranch heatmap

# %%
# parameters

persist_species_time_heatmap_params = dict()

# %%
# call the task


persist_species_time_heatmap = (
    persist_text.set_task_instance_id("persist_species_time_heatmap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="species_by_time_heatmap",
        **persist_species_time_heatmap_params,
    )
    .mapvalues(argnames=["text"], argvalues=species_time_heatmap)
)


# %% [markdown]
# ## Draw multi-bar time series chart of animals killed by ranch

# %%
# parameters

ranch_killed_multibar_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


ranch_killed_multibar_chart = (
    draw_custom_multi_bar_time_series.set_task_instance_id(
        "ranch_killed_multibar_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="date",
        y_axis="total_killed",
        group_column="ranch",
        agg_function="sum",
        time_frequency=time_frequency,
        ncols=2,
        shared_yaxes=False,
        group_order=None,
        row_height=400,
        ascending=True,
        bar_color="#6495ed",
        x_axis_style={"title": "Date"},
        y_axis_style={"title": "Total animals killed"},
        tick_font_size=None,
        plot_style=None,
        layout_style=None,
        **ranch_killed_multibar_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist multi-bar time series chart of animals killed by ranch

# %%
# parameters

persist_ranch_killed_multibar_params = dict()

# %%
# call the task


persist_ranch_killed_multibar = (
    persist_text.set_task_instance_id("persist_ranch_killed_multibar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_species_killed_ranch_multibar",
        **persist_ranch_killed_multibar_params,
    )
    .mapvalues(argnames=["text"], argvalues=ranch_killed_multibar_chart)
)


# %% [markdown]
# ## Draw multi-bar time series chart of animals killed by species

# %%
# parameters

species_killed_multibar_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


species_killed_multibar_chart = (
    draw_custom_multi_bar_time_series.set_task_instance_id(
        "species_killed_multibar_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="date",
        y_axis="total_killed",
        group_column="species_killed",
        agg_function="sum",
        time_frequency=time_frequency,
        ncols=2,
        shared_yaxes=False,
        group_order=None,
        row_height=400,
        ascending=True,
        bar_color="#6495ed",
        x_axis_style={"title": "Date"},
        y_axis_style={"title": "Total animals killed"},
        tick_font_size=None,
        plot_style=None,
        layout_style=None,
        **species_killed_multibar_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist multi-bar time series chart of animals killed by species

# %%
# parameters

persist_species_killed_multibar_params = dict()

# %%
# call the task


persist_species_killed_multibar = (
    persist_text.set_task_instance_id("persist_species_killed_multibar")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_species_killed_multibar",
        **persist_species_killed_multibar_params,
    )
    .mapvalues(argnames=["text"], argvalues=species_killed_multibar_chart)
)


# %% [markdown]
# ## Draw multi-line chart of total animals killed by ranch

# %%
# parameters

ranch_killed_multiline_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


ranch_killed_multiline_chart = (
    draw_custom_multi_line_time_series.set_task_instance_id(
        "ranch_killed_multiline_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="date",
        y_axis="total_killed",
        group_column="ranch",
        agg_function="sum",
        color_column="ranch_colors",
        line_style={"shape": "linear", "dash": "solid"},
        fill=False,
        ascending=True,
        plot_style=None,
        group_order=None,
        time_frequency=time_frequency,
        layout_style={
            "font_size": 15,
            "legend_title": "Ranch",
            "font_style": "normal",
            "font_color": "#222222",
            "showlegend": True,
            "hovermode": "x unified",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {
                "title": "Date",
                "tickangle": 45,
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
            "yaxis": {
                "title": "Number of livestock killed",
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
        },
        **ranch_killed_multiline_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist multi-line chart of total animals killed by ranch

# %%
# parameters

persist_ranch_killed_multiline_params = dict()

# %%
# call the task


persist_ranch_killed_multiline = (
    persist_text.set_task_instance_id("persist_ranch_killed_multiline")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_killed_over_time_by_ranch_chart",
        **persist_ranch_killed_multiline_params,
    )
    .mapvalues(argnames=["text"], argvalues=ranch_killed_multiline_chart)
)


# %% [markdown]
# ## Draw multi-line chart of total animals killed by species

# %%
# parameters

species_killed_multiline_chart_params = dict(
    widget_id=...,
)

# %%
# call the task


species_killed_multiline_chart = (
    draw_custom_multi_line_time_series.set_task_instance_id(
        "species_killed_multiline_chart"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="date",
        y_axis="total_killed",
        group_column="species_killed",
        agg_function="sum",
        color_column="species_killed_colors",
        line_style={"shape": "linear", "dash": "solid"},
        fill=False,
        ascending=True,
        plot_style=None,
        group_order=None,
        time_frequency=time_frequency,
        layout_style={
            "font_size": 15,
            "legend_title": "Species",
            "font_style": "normal",
            "font_color": "#222222",
            "showlegend": True,
            "hovermode": "x unified",
            "plot_bgcolor": "#f5f5f5",
            "title_x": 0.5,
            "xaxis": {
                "title": "Date",
                "tickangle": 45,
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
            "yaxis": {
                "title": "Number of livestock killed",
                "showgrid": True,
                "gridcolor": "#e5e5e5",
            },
        },
        **species_killed_multiline_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Persist multi-line chart of total animals killed by ranch

# %%
# parameters

persist_species_killed_multiline_params = dict()

# %%
# call the task


persist_species_killed_multiline = (
    persist_text.set_task_instance_id("persist_species_killed_multiline")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filename_suffix="livestock_killed_over_time_by_species_chart",
        **persist_species_killed_multiline_params,
    )
    .mapvalues(argnames=["text"], argvalues=species_killed_multiline_chart)
)


# %% [markdown]
# ## Remove Geographic Outliers from Events

# %%
# parameters

exclude_event_outliers_params = dict()

# %%
# call the task


exclude_event_outliers = (
    exclude_geom_outliers.set_task_instance_id("exclude_event_outliers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(z_threshold=3, **exclude_event_outliers_params)
    .mapvalues(argnames=["df"], argvalues=split_events_by_group)
)


# %% [markdown]
# ## Remove invalid points from events

# %%
# parameters

remove_invalid_geoms_params = dict()

# %%
# call the task


remove_invalid_geoms = (
    drop_null_geometry.set_task_instance_id("remove_invalid_geoms")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(geometry_column="geometry", **remove_invalid_geoms_params)
    .mapvalues(argnames=["gdf"], argvalues=exclude_event_outliers)
)


# %% [markdown]
# ## Generate livestock predation event scatterplot layer

# %%
# parameters

generate_livestock_layer_params = dict()

# %%
# call the task


generate_livestock_layer = (
    create_scatterplot_layer.set_task_instance_id("generate_livestock_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data_url=None,
        layer_style={
            "get_fill_color": "species_killed_colors",
            "get_line_color": "species_killed_colors",
            "get_radius": 4,
            "opacity": 0.75,
            "stroked": True,
        },
        legend={
            "title": "Livestock species",
            "label_column": "species_killed",
            "color_column": "species_killed_colors",
            "sort": "ascending",
            "label_suffix": None,
        },
        **generate_livestock_layer_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=remove_invalid_geoms)
)


# %% [markdown]
# ## Overlay Livestock Events on Amboseli Map

# %%
# parameters

combine_ambo_lvs_layers_params = dict()

# %%
# call the task


combine_ambo_lvs_layers = (
    combine_deckgl_map_layers.set_task_instance_id("combine_ambo_lvs_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[custom_amboseli_layer, custom_protected_layer],
        **combine_ambo_lvs_layers_params,
    )
    .mapvalues(argnames=["grouped_layers"], argvalues=generate_livestock_layer)
)


# %% [markdown]
# ## Calculate Zoom Level for Livestock Event Map

# %%
# parameters

global_zoom_value_params = dict()

# %%
# call the task


global_zoom_value = (
    view_state_deck_gdf.set_task_instance_id("global_zoom_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        pitch=0, bearing=0, gdf=reproject_ambo_boundaries, **global_zoom_value_params
    )
    .call()
)


# %% [markdown]
# ## Combine livestock predation layers with view state

# %%
# parameters

zip_predation_with_viewstate_params = dict()

# %%
# call the task


zip_predation_with_viewstate = (
    zip_groupbykey.set_task_instance_id("zip_predation_with_viewstate")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_ambo_lvs_layers, global_zoom_value],
        **zip_predation_with_viewstate_params,
    )
    .call()
)


# %% [markdown]
# ## Draw livestock predation event map

# %%
# parameters

draw_livestock_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_livestock_map = (
    draw_map.set_task_instance_id("draw_livestock_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_livestock_map_params,
    )
    .mapvalues(
        argnames=["geo_layers", "view_state"], argvalues=zip_predation_with_viewstate
    )
)


# %% [markdown]
# ## Persist livestock predation html

# %%
# parameters

persist_livestock_html_params = dict(
    filename=...,
)

# %%
# call the task


persist_livestock_html = (
    persist_text.set_task_instance_id("persist_livestock_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="livestock_predation_map",
        **persist_livestock_html_params,
    )
    .mapvalues(argnames=["text"], argvalues=draw_livestock_map)
)


# %% [markdown]
# ## Generate density grid

# %%
# parameters

generate_predation_grids_params = dict()

# %%
# call the task


generate_predation_grids = (
    generate_density_grid.set_task_instance_id("generate_predation_grids")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        cell_size_meters=2000, geometry_type="point", **generate_predation_grids_params
    )
    .mapvalues(argnames=["features_gdf"], argvalues=remove_invalid_geoms)
)


# %% [markdown]
# ## Sort density grid values in ascending order

# %%
# parameters

sort_density_grid_params = dict()

# %%
# call the task


sort_density_grid = (
    sort_values.set_task_instance_id("sort_density_grid")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column_name="density",
        ascending=True,
        na_position="last",
        **sort_density_grid_params,
    )
    .mapvalues(argnames=["df"], argvalues=generate_predation_grids)
)


# %% [markdown]
# ## Apply bin classification to density grid

# %%
# parameters

density_bins_params = dict()

# %%
# call the task


density_bins = (
    apply_classification.set_task_instance_id("density_bins")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="density",
        output_column_name="density_bins",
        label_options={"label_ranges": True, "label_decimals": 0},
        classification_options={"scheme": "equal_interval", "k": 5},
        **density_bins_params,
    )
    .mapvalues(argnames=["df"], argvalues=sort_density_grid)
)


# %% [markdown]
# ## Apply colormap to density bins

# %%
# parameters

apply_density_colormap_params = dict()

# %%
# call the task


apply_density_colormap = (
    apply_color_map.set_task_instance_id("apply_density_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="density_bins",
        output_column_name="density_bins_colors",
        colormap=["#FFF7BC", "#FD8D3C", "#F03B20", "#BD0026", "#99000D"],
        **apply_density_colormap_params,
    )
    .mapvalues(argnames=["df"], argvalues=density_bins)
)


# %% [markdown]
# ## Create density grid layers

# %%
# parameters

generate_density_layers_params = dict()

# %%
# call the task


generate_density_layers = (
    create_geojson_layer.set_task_instance_id("generate_density_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        data_url=None,
        layer_style={
            "filled": True,
            "stroked": True,
            "extruded": False,
            "wireframe": False,
            "get_fill_color": "density_bins_colors",
            "get_line_color": [0, 0, 0, 200],
            "opacity": 0.55,
            "get_line_width": 0.35,
            "get_elevation": 0,
            "get_point_radius": 1,
            "line_width_units": "pixels",
            "line_width_scale": 1,
            "line_width_min_pixels": 1,
            "line_width_max_pixels": 5,
        },
        legend={
            "title": "Density",
            "label_column": "density_bins",
            "color_column": "density_bins_colors",
            "sort": None,
            "label_suffix": None,
        },
        **generate_density_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=apply_density_colormap)
)


# %% [markdown]
# ## Overlay Predation Density Grid on Amboseli Map

# %%
# parameters

combine_ambo_grid_layers_params = dict()

# %%
# call the task


combine_ambo_grid_layers = (
    combine_deckgl_map_layers.set_task_instance_id("combine_ambo_grid_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[custom_amboseli_layer, custom_protected_layer],
        **combine_ambo_grid_layers_params,
    )
    .mapvalues(argnames=["grouped_layers"], argvalues=generate_density_layers)
)


# %% [markdown]
# ## Combine grid layers with view state

# %%
# parameters

zip_grid_with_viewstate_params = dict()

# %%
# call the task


zip_grid_with_viewstate = (
    zip_groupbykey.set_task_instance_id("zip_grid_with_viewstate")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_ambo_grid_layers, global_zoom_value],
        **zip_grid_with_viewstate_params,
    )
    .call()
)


# %% [markdown]
# ## Draw grid event map

# %%
# parameters

draw_grid_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_grid_map = (
    draw_map.set_task_instance_id("draw_grid_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_grid_map_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_grid_with_viewstate)
)


# %% [markdown]
# ## Persist grid html

# %%
# parameters

persist_grid_html_params = dict(
    filename=...,
)

# %%
# call the task


persist_grid_html = (
    persist_text.set_task_instance_id("persist_grid_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="density_grid_map",
        **persist_grid_html_params,
    )
    .mapvalues(argnames=["text"], argvalues=draw_grid_map)
)


# %% [markdown]
# ## Create predation density widgets

# %%
# parameters

create_predation_widgets_params = dict()

# %%
# call the task


create_predation_widgets = (
    create_map_widget_single_view.set_task_instance_id("create_predation_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Predation points", **create_predation_widgets_params)
    .map(argnames=["view", "data"], argvalues=persist_livestock_html)
)


# %% [markdown]
# ## Merge density map widgets

# %%
# parameters

merge_predation_map_widgets_params = dict()

# %%
# call the task


merge_predation_map_widgets = (
    merge_widget_views.set_task_instance_id("merge_predation_map_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=create_predation_widgets, **merge_predation_map_widgets_params)
    .call()
)


# %% [markdown]
# ## Create density widgets

# %%
# parameters

create_density_widgets_params = dict()

# %%
# call the task


create_density_widgets = (
    create_map_widget_single_view.set_task_instance_id("create_density_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Predation Density", **create_density_widgets_params)
    .map(argnames=["view", "data"], argvalues=persist_grid_html)
)


# %% [markdown]
# ## Merge density widgets

# %%
# parameters

merge_density_map_widgets_params = dict()

# %%
# call the task


merge_density_map_widgets = (
    merge_widget_views.set_task_instance_id("merge_density_map_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=create_density_widgets, **merge_density_map_widgets_params)
    .call()
)


# %% [markdown]
# ## Create Plot Widget for total livestock killed pie chart

# %%
# parameters

total_livestock_pie_chart_widget_params = dict()

# %%
# call the task


total_livestock_pie_chart_widget = (
    create_plot_widget_single_view.set_task_instance_id(
        "total_livestock_pie_chart_widget"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(
        title="Total livestock killed by species",
        **total_livestock_pie_chart_widget_params,
    )
    .map(argnames=["view", "data"], argvalues=persist_total_livestock_pie)
)


# %% [markdown]
# ## Merge Pie chart Widget Views

# %%
# parameters

grouped_killed_pie_widget_merge_params = dict()

# %%
# call the task


grouped_killed_pie_widget_merge = (
    merge_widget_views.set_task_instance_id("grouped_killed_pie_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        widgets=total_livestock_pie_chart_widget,
        **grouped_killed_pie_widget_merge_params,
    )
    .call()
)


# %% [markdown]
# ## Create Plot Widget for ranch pie chart

# %%
# parameters

ranch_pie_chart_widget_params = dict()

# %%
# call the task


ranch_pie_chart_widget = (
    create_plot_widget_single_view.set_task_instance_id("ranch_pie_chart_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Total livestock killed by ranch", **ranch_pie_chart_widget_params)
    .map(argnames=["view", "data"], argvalues=persist_ranch_livestock_pie)
)


# %% [markdown]
# ## Merge Pie chart Widget Views

# %%
# parameters

grouped_ranch_pie_widget_merge_params = dict()

# %%
# call the task


grouped_ranch_pie_widget_merge = (
    merge_widget_views.set_task_instance_id("grouped_ranch_pie_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=ranch_pie_chart_widget, **grouped_ranch_pie_widget_merge_params)
    .call()
)


# %% [markdown]
# ## Create Plot Widget for species heatmap

# %%
# parameters

species_heatmap_widget_params = dict()

# %%
# call the task


species_heatmap_widget = (
    create_plot_widget_single_view.set_task_instance_id("species_heatmap_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Livestock species killed by ranch", **species_heatmap_widget_params)
    .map(argnames=["view", "data"], argvalues=persist_species_ranch_heatmap)
)


# %% [markdown]
# ## Merge species heatmap Widget Views

# %%
# parameters

species_heat_widget_merge_params = dict()

# %%
# call the task


species_heat_widget_merge = (
    merge_widget_views.set_task_instance_id("species_heat_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=species_heatmap_widget, **species_heat_widget_merge_params)
    .call()
)


# %% [markdown]
# ## Create Plot Widget for species heatmap

# %%
# parameters

time_heatmap_widget_params = dict()

# %%
# call the task


time_heatmap_widget = (
    create_plot_widget_single_view.set_task_instance_id("time_heatmap_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Livestock species killed by time", **time_heatmap_widget_params)
    .map(argnames=["view", "data"], argvalues=persist_species_time_heatmap)
)


# %% [markdown]
# ## Merge species heatmap Widget Views

# %%
# parameters

grouped_time_heat_widget_merge_params = dict()

# %%
# call the task


grouped_time_heat_widget_merge = (
    merge_widget_views.set_task_instance_id("grouped_time_heat_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=time_heatmap_widget, **grouped_time_heat_widget_merge_params)
    .call()
)


# %% [markdown]
# ## Create Plot Widget for multiline

# %%
# parameters

ranch_line_widget_params = dict()

# %%
# call the task


ranch_line_widget = (
    create_plot_widget_single_view.set_task_instance_id("ranch_line_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Livestock species killed by ranch", **ranch_line_widget_params)
    .map(argnames=["view", "data"], argvalues=persist_ranch_killed_multiline)
)


# %% [markdown]
# ## Merge ranch multiline Widget Views

# %%
# parameters

grouped_ranch_line_widget_merge_params = dict()

# %%
# call the task


grouped_ranch_line_widget_merge = (
    merge_widget_views.set_task_instance_id("grouped_ranch_line_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=ranch_line_widget, **grouped_ranch_line_widget_merge_params)
    .call()
)


# %% [markdown]
# ## Create Plot Widget for multiline

# %%
# parameters

species_line_widget_params = dict()

# %%
# call the task


species_line_widget = (
    create_plot_widget_single_view.set_task_instance_id("species_line_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Livestock species killed by species", **species_line_widget_params)
    .map(argnames=["view", "data"], argvalues=persist_species_killed_multiline)
)


# %% [markdown]
# ## Merge ranch multiline Widget Views

# %%
# parameters

species_line_widget_merge_params = dict()

# %%
# call the task


species_line_widget_merge = (
    merge_widget_views.set_task_instance_id("species_line_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=species_line_widget, **species_line_widget_merge_params)
    .call()
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_livestock_pie_png_params = dict()

# %%
# call the task


convert_livestock_pie_png = (
    html_to_png.set_task_instance_id("convert_livestock_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_livestock_pie_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_total_livestock_pie)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_ranch_pie_png_params = dict()

# %%
# call the task


convert_ranch_pie_png = (
    html_to_png.set_task_instance_id("convert_ranch_pie_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_ranch_pie_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_ranch_livestock_pie)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_species_heat_png_params = dict()

# %%
# call the task


convert_species_heat_png = (
    html_to_png.set_task_instance_id("convert_species_heat_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_species_heat_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_species_ranch_heatmap)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_species_time_png_params = dict()

# %%
# call the task


convert_species_time_png = (
    html_to_png.set_task_instance_id("convert_species_time_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_species_time_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_species_time_heatmap)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_ranch_multi_png_params = dict()

# %%
# call the task


convert_ranch_multi_png = (
    html_to_png.set_task_instance_id("convert_ranch_multi_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "width": 1280,
            "height": 2000,
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_ranch_multi_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_ranch_killed_multibar)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_species_multi_png_params = dict()

# %%
# call the task


convert_species_multi_png = (
    html_to_png.set_task_instance_id("convert_species_multi_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "width": 1280,
            "height": 2000,
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_species_multi_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_species_killed_multibar)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_ranch_line_png_params = dict()

# %%
# call the task


convert_ranch_line_png = (
    html_to_png.set_task_instance_id("convert_ranch_line_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_ranch_line_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_ranch_killed_multiline)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_species_line_png_params = dict()

# %%
# call the task


convert_species_line_png = (
    html_to_png.set_task_instance_id("convert_species_line_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **convert_species_line_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_species_killed_multiline)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_livestock_png_params = dict()

# %%
# call the task


convert_livestock_png = (
    html_to_png.set_task_instance_id("convert_livestock_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_livestock_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_livestock_html)
)


# %% [markdown]
# ## Convert html to png

# %%
# parameters

convert_grid_png_params = dict()

# %%
# call the task


convert_grid_png = (
    html_to_png.set_task_instance_id("convert_grid_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_grid_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_grid_html)
)


# %% [markdown]
# ## MEP monthly report  dashboard

# %%
# parameters

mep_monthly_dashboard_params = dict(
    warning=...,
)

# %%
# call the task


mep_monthly_dashboard = (
    gather_dashboard.set_task_instance_id("mep_monthly_dashboard")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        details=workflow_details,
        widgets=[
            merge_predation_map_widgets,
            merge_density_map_widgets,
            grouped_killed_pie_widget_merge,
            grouped_ranch_pie_widget_merge,
            species_heat_widget_merge,
            grouped_time_heat_widget_merge,
            grouped_ranch_line_widget_merge,
            species_line_widget_merge,
        ],
        time_range=time_range,
        groupers=groupers,
        **mep_monthly_dashboard_params,
    )
    .call()
)
